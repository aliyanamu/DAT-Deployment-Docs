# dynamic CORS handling. previously this part is used to handle
# our preview development server that does served using static
# domain name
map $http_origin $cors_origin {
  default "";

  ~^https://desk.api.domain.tld $http_origin;   # admin dashboard
  
  # add other regex here as needed
}

upstream nespay_job_upstream {
  server localhost:8002;
}

server {
  listen         80;
  server_name    job.domain.tld;

  access_log     /var/log/nginx/job.domain.tld-access.log;
  error_log      /var/log/nginx/job.domain.tld-error.log;

  location / {
    proxy_pass http://nespay_job_upstream;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Authorization $http_authorization;

    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass_request_headers on;

    #: dynamically handle cors origin.
    add_header 'Access-Control-Allow-Origin' $cors_origin always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PATCH, PUT, DELETE' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization,Content-Type,Origin,Accept,Cookie,x-request-id, x-idempotency-key' always;

    # This OPTIONS block is for handling CORS preflight stuff.
    # Make sure 'Access-Control-Allow-Credentials' stays set to 'true'
    # cause the frontend sends cookies/auth and browser will block if it’s missing.
    if ($request_method = OPTIONS) {
      add_header 'Access-Control-Allow-Origin' $cors_origin always;
      add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PATCH, PUT, DELETE' always;
      add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Origin, Accept, Cookie, x-request-id, x-idempotency-key' always;
      add_header 'Access-Control-Allow-Credentials' 'true' always;
      add_header 'Access-Control-Max-Age' 1728000;
      return 204;
    }
  }
}

